name: CI

on:
  pull_request:
  push:

# Cancel older runs on the same branch/PR so only the latest runs.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Python deps
        if: ${{ hashFiles('tests/**/*.py') != '' }}
        run: |
          python -m pip install --upgrade pip
          pip install pytest psutil
      - name: Run tests
        if: ${{ hashFiles('tests/**/*.py') != '' }}
        run: |
          # Run only tests that don't require environment variables
          pytest tests/test_emoji_patterns.py tests/test_system_health.py -v

  scaffold-checks:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Ensure scaffold exists
        run: |
          test -d src || true
          test -d tests || true
          test -f README.md || true
          test -f docs/OVERVIEW.md || true
          test -f docs/ARCHITECTURE.md || true
          test -f docs/CAPABILITIES.md || true
          test -f docs/SAFETY.md || true
          test -f ops/RUNBOOK.md || true
